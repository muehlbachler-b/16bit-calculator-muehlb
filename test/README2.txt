The testbench of the template tb.v is just so the github actions are passing.

The real testbench to make simulations is calculator_tb.v (it is a little bit like a template so you can easily choose
                                                           the alu operation selection as well as the data of the operands
                                                           A and B).
